packages:
  - "packages/*"
  - "apps/*"

# Allow only these dependencies to run install scripts (pnpm 10 blocks them by default).
# esbuild ships a platform-specific binary via its postinstall and is required by tsup.
onlyBuiltDependencies:
  - esbuild
