node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.turbo/
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json

# code-pact: local-only planning notes (kept out of the public repo)
.local/
# code-pact: regenerable per-task context packs
.context/
# code-pact: transient write locks (only ever populated mid-command)
.code-pact/locks/
# code-pact: local loop-memory cache
.code-pact/cache/

# Publish assets copied into apps/cli by prepack (sources live at repo root)
apps/cli/LICENSE
apps/cli/NOTICE
packages/sdk/LICENSE
packages/sdk/NOTICE

# restored PR #56 placeholder; removed by follow-up direct cleanup
