node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.idea/
.vscode/

# A VS Code build `@vscode/test-electron` downloads for `pnpm smoke:vscode`. Test-time only, and
# never checked in: it is an application, not a fixture.
.vscode-test/

# Failure artifacts from `pnpm smoke:chrome` — a screenshot and the popup's text, written only when
# the smoke fails, and uploaded by CI rather than committed.
.chrome-host-smoke/

# Personal coding-agent configuration (not a shared project contract)
CLAUDE.local.md
.claude/

# Publish assets copied into apps/cli by prepack (sources live at repo root)
apps/cli/LICENSE
apps/cli/NOTICE
packages/sdk/LICENSE
packages/sdk/NOTICE
docs/generated/corpus-evaluation.json
docs/generated/risk-index-calibration.json
