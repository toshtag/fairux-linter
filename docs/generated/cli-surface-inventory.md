# CLI public surface inventory

<!-- Generated by scripts/generate-cli-inventory.mjs. Do not edit by hand. -->

> Generated from apps/cli/src/cli-surface.ts, as Commander parses it. Not the help text.

A command, a flag, a short alias, a value arity, a default, or an accepted value leaving this
file is a breaking change. Arriving is not.

## Exit codes

| Code | Meaning |
| --- | --- |
| 0 | the command did what it was asked, and nothing it was told to fail on was found. A scan that reports findings still exits 0 unless --fail-on names a severity they meet |
| 1 | the run failed, or a finding met --fail-on. Also a --fix-write that was asked for and did not land, and a target that matched no scannable file |
| 2 | the invocation is wrong and nothing happened: an unknown value, two flags that contradict each other, a flag this run would accept and ignore, an unknown rule id, or a journey file that is not there. No discovery, no scan, no RulePack import, no output file |

## `fairux scan`

Arguments: `path`

15 options.

| Option | Value | Notes |
| --- | --- | --- |
| `--format` | required | alias `-f`; default `"markdown"`; one of `json`, `markdown`, `sarif`, `html` |
| `--include-experimental` | — | — |
| `--config` | required | — |
| `--ignore-config` | — | default `false` |
| `--stdin-filename` | required | — |
| `--no-ignore` | — | negates |
| `--baseline` | required | — |
| `--write-baseline` | required | — |
| `--suppress` | required | — |
| `--rule-pack` | required | repeatable |
| `--fail-on` | required | one of `high`, `medium`, `low`, `info` |
| `--risk-index` | required | — |
| `--risk-index-model` | required | — |
| `--fix-dry-run` | — | — |
| `--fix-write` | — | — |

## `fairux scan-journey`

Arguments: `file`

6 options.

| Option | Value | Notes |
| --- | --- | --- |
| `--format` | required | alias `-f`; default `"markdown"`; one of `json`, `markdown` |
| `--include-experimental` | — | — |
| `--config` | required | — |
| `--ignore-config` | — | default `false` |
| `--rule-pack` | required | repeatable |
| `--fail-on` | required | one of `high`, `medium`, `low`, `info` |

## `fairux rules`

6 options.

| Option | Value | Notes |
| --- | --- | --- |
| `--format` | required | alias `-f`; default `"text"`; one of `text`, `json` |
| `--include-experimental` | — | — |
| `--config` | required | — |
| `--ignore-config` | — | default `false` |
| `--rule-pack` | required | repeatable |
| `--runtime` | required | one of `html`, `dom`, `ast`, `figma` |

## `fairux explain`

Arguments: `rule-id`

5 options.

| Option | Value | Notes |
| --- | --- | --- |
| `--format` | required | alias `-f`; default `"text"`; one of `text`, `json` |
| `--include-experimental` | — | — |
| `--config` | required | — |
| `--ignore-config` | — | default `false` |
| `--rule-pack` | required | repeatable |
